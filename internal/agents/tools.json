[
  {
    "name": "read_file",
    "description": "Read file by line range (1-indexed) or entire file. LARGE FILE PROTECTION: (1) Files > 500MB — refused, use grep first. (2) Files > 10MB — should_read_entire_file=true blocked, use line ranges. (3) Entire reads capped at 500 lines / 10KB (excess truncated, truncated=true). (4) Files > 2MB trigger warning. Response: content, lines, file_size_bytes, total_lines, truncated, warning, read_mode; range reads also add start_line, end_line, lines_before_range, lines_after_range. ALWAYS check file_size_bytes and total_lines before further reads. Max 500 lines/call for range reads. should_read_entire_file=true only for files < 2MB, < 500 lines. For large files, use start_line_one_indexed + end_line_one_indexed_inclusive to paginate. If truncated=true, read remaining with line ranges.",
    "parameters": {
      "type": "object",
      "properties": {
        "target_file": {
          "type": "string",
          "description": "File path to read (relative or absolute)."
        },
        "start_line_one_indexed": {
          "type": "integer",
          "description": "1-indexed start line (inclusive). Required when should_read_entire_file=false. Default: 1."
        },
        "end_line_one_indexed_inclusive": {
          "type": "integer",
          "description": "1-indexed end line (inclusive). Max 500 lines from start_line. Required when should_read_entire_file=false. Default: start_line + 499."
        },
        "should_read_entire_file": {
          "type": "boolean",
          "description": "Read entire file. BLOCKED for > 10MB files. Capped at 500 lines / 10KB. Only for < 2MB, < 500 line files. Default: false."
        },
        "explanation": {
          "type": "string",
          "description": "Why this tool is used and how it contributes to the goal."
        }
      },
      "required": [
        "target_file",
        "should_read_entire_file",
        "start_line_one_indexed",
        "end_line_one_indexed_inclusive"
      ]
    }
  },
  {
    "name": "search_replace_in_file",
    "description": "Replace text in a file. Provide file_path (absolute), old_string (exact match), and new_string. For moves/renames, use run_bash with mv.\n\nCRITICAL: old_string must be unique — include 3-5 lines of context. One replacement per call; use multiple calls for multiple changes. To create a new file, use empty old_string with full content as new_string.\n\nVerify with read_file before editing. Absolute paths only.",
    "parameters": {
      "type": "object",
      "properties": {
        "file_path": {
          "type": "string",
          "description": "Absolute file path to modify."
        },
        "old_string": {
          "type": "string",
          "description": "Exact text to replace (must be unique in file and match contents exactly, including whitespace and indentation)."
        },
        "new_string": {
          "type": "string",
          "description": "Replacement text."
        }
      },
      "required": [
        "file_path",
        "old_string",
        "new_string"
      ]
    }
  },
  {
    "description": "Create a new file at the specified path with given content. Fails if the file already exists.",
    "name": "create_file",
    "parameters": {
      "properties": {
        "target_file": {
          "description": "File path to create (relative or absolute; absolute paths are preserved as-is.",
          "type": "string"
        },
        "content": {
          "description": "Content to write to the new file.",
          "type": "string"
        }
      },
      "required": [
        "target_file",
        "content"
      ],
      "type": "object"
    }
  },
  {
    "name": "run_bash",
    "description": "Execute a bash command. Use is_dangerous=true for commands affecting anything beyond the workspace (system packages, services, network, sudo). Use is_dangerous=false only for workspace operations (build, lint, test, git). Use is_background=true for long-running commands.\n\nGuidelines: use absolute paths, chain with && for sequential execution, avoid newlines, append | cat for paged output. Dangerous commands require user authorization.",
    "parameters": {
      "type": "object",
      "properties": {
        "command": {
          "type": "string",
          "description": "Bash command to execute."
        },
        "explanation": {
          "type": "string",
          "description": "Why this command is needed and how it contributes to the goal."
        },
        "is_background": {
          "type": "boolean",
          "description": "Run the command in the background."
        },
        "is_dangerous": {
          "type": "boolean",
          "description": "Whether the command affects anything beyond the project workspace (files outside project, system config, running processes, installed packages, network settings, dotfiles/shell config). When in doubt, set to true."
        }
      },
      "required": [
        "command",
        "is_background",
        "is_dangerous"
      ]
    }
  },
  {
    "name": "thinking",
    "description": "Cognitive tool for self-correction and deep analysis. Use IMMEDIATELY when an action fails, an error occurs, or you're unsure. Do NOT use to execute actions (write files, run commands). Instead, generate a 'Chain of Thought' log: 1. Analyze root cause. 2. Brainstorm 2-3 solutions with pros/cons. 3. Select best solution. Avoids blind retries and infinite loops.",
    "parameters": {
      "type": "object",
      "properties": {
        "problem_description": {
          "type": "string",
          "description": "Error message, failure description, or problem to solve."
        },
        "current_action": {
          "type": "string",
          "description": "Action attempted and failed, or current context."
        },
        "observation": {
          "type": "string",
          "description": "Additional context, observations, or what happened."
        }
      },
      "required": [
        "problem_description",
        "current_action"
      ]
    }
  },
  {
    "name": "micro_agent",
    "description": "Isolated LLM inference call with a custom system prompt. Use for reasoning, analysis, or generation that benefits from a fresh context. Typical uses: deep code/error analysis, structured output (JSON, summaries, translations), self-reflection. Returns raw text.",
    "parameters": {
      "type": "object",
      "properties": {
        "system_prompt": {
          "type": "string",
          "description": "System prompt defining role, rules, and output format for this LLM inference."
        },
        "task": {
          "type": "string",
          "description": "Task description, question, or content to process."
        }
      },
      "required": [
        "system_prompt",
        "task"
      ]
    }
  },
  {
    "name": "deepthinking",
    "description": "Deep system analysis for complex problems. Multi-dimensional analysis (root cause, system impact, constraints, solution architecture) via isolated LLM call. Use ONLY when: 1) conventional methods (thinking, micro_agent, repo analysis, sub-agent delegation) have failed, 2) the problem requires systematic design, or 3) multiple failures indicate fundamental misunderstanding. Skip for simple issues. Input: context (problem context, errors, constraints, what was tried) and goal (specific objective).",
    "parameters": {
      "type": "object",
      "properties": {
        "context": {
          "type": "string",
          "description": "Complete problem context: execution feedback, key errors, problem background, constraints, what was tried and failed, and any other relevant info for deep analysis."
        },
        "goal": {
          "type": "string",
          "description": "Specific objective. What should the deep analysis produce? A solution design, debugging strategy, architectural decision, or comprehensive plan."
        }
      },
      "required": [
        "context",
        "goal"
      ]
    }
  },
  {
    "name": "delete_file",
    "description": "Delete files (multiple in one call). Must ensure files exist before deleting.",
    "parameters": {
      "type": "object",
      "properties": {
        "file_paths": {
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      },
      "required": [
        "file_paths"
      ]
    }
  },
  {
    "name": "rename_file",
    "description": "Move or rename an existing file.",
    "parameters": {
      "type": "object",
      "properties": {
        "file_path": {
          "type": "string"
        },
        "rename_file_path": {
          "type": "string"
        }
      },
      "required": [
        "file_path",
        "rename_file_path"
      ]
    }
  },
  {
    "name": "list_dir",
    "description": "View files in the specified directory.",
    "parameters": {
      "type": "object",
      "properties": {
        "dir_path": {
          "type": "string"
        },
        "max_depth": {
          "type": "integer",
          "default": 3
        }
      },
      "required": [
        "dir_path"
      ]
    }
  },
  {
    "name": "print_dir_tree",
    "description": "Print directory tree. Auto-ignores common build artifacts, cache dirs, dependency folders, compiled files. show_files=false for directory-only overview (with file counts). Output truncated per-directory when exceeding max_items. Depth hard-capped at 2 levels.",
    "parameters": {
      "type": "object",
      "properties": {
        "dir_path": {
          "type": "string",
          "description": "Directory path to print tree for."
        },
        "max_depth": {
          "type": "integer",
          "default": 2,
          "description": "Max depth to traverse. Hard-capped at 2 (values > 2 clamped). Depth 0 shows only root name. Default: 2"
        },
        "show_files": {
          "type": "boolean",
          "default": true,
          "description": "Show individual files (true) or only directories (false). When false, file counts shown per directory. Default: true"
        },
        "max_items": {
          "type": "integer",
          "default": 50,
          "description": "Max items to display per directory. Extra items truncated with a count. Default: 50"
        },
        "ignore_extra": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [],
          "description": "Additional dirs to ignore (merged with built-in defaults). Useful for project-specific noise."
        }
      },
      "required": [
        "dir_path"
      ]
    }
  },
  {
    "name": "search_by_regex",
    "description": "High-performance search via ripgrep (rg) for large codebases. Supports PCRE2 regex and respects .gitignore. Locate function definitions, variable references, error messages, or complex code patterns across files.",
    "parameters": {
      "type": "object",
      "properties": {
        "query": {
          "type": "string"
        },
        "search_directory": {
          "type": "string"
        }
      },
      "required": [
        "query"
      ]
    }
  },
  {
    "name": "semantic_search",
    "description": "Semantic search for relevant code snippets via natural language query or code snippet. Returns matches with file paths, symbol names, and code blocks. Use when you need specific functionality/implementation details but don't know the exact file or function name.",
    "parameters": {
      "type": "object",
      "properties": {
        "query": {
          "type": "string",
          "description": "Natural language query or code snippet to search for."
        },
        "limit": {
          "type": "integer",
          "description": "Max results to return. Default: 5.",
          "default": 5
        }
      },
      "required": [
        "query"
      ]
    }
  },
  {
    "name": "query_code_skeleton",
    "description": "Get skeleton (function/class definitions without implementation) of specific files. Use to understand structure without reading full content.",
    "parameters": {
      "type": "object",
      "properties": {
        "filepaths": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Absolute file paths to query skeletons for."
        }
      },
      "required": [
        "filepaths"
      ]
    }
  },
  {
    "name": "query_code_snippet",
    "description": "Get code snippet of a specific function in a file. Use when you need to read the function's implementation.",
    "parameters": {
      "type": "object",
      "properties": {
        "filepath": {
          "type": "string",
          "description": "Absolute path to the file."
        },
        "function_name": {
          "type": "string",
          "description": "Function name to retrieve."
        }
      },
      "required": [
        "filepath",
        "function_name"
      ]
    }
  },
  {
    "name": "ask_user_for_help",
    "description": "Use when you need user input, confirmation, or selection. Pauses execution until user responds. Three modes: confirm (yes/no), select (choose from options), input (free text). Mode auto-inferred from suggested_options or set via interaction_type.",
    "parameters": {
      "type": "object",
      "properties": {
        "reason": {
          "type": "string",
          "description": "Why you're stuck and need user help/authorization."
        },
        "specific_question": {
          "type": "string",
          "description": "Specific question to ask the user (shown directly to user."
        },
        "suggested_options": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Optional answer options for the user. Controls interaction mode: empty → input (free text), two boolean-like options (e.g., ['yes', 'no']) → confirm (yes/no), 2+ non-boolean options → select (option list)."
        },
        "interaction_type": {
          "type": "string",
          "enum": [
            "confirm",
            "select",
            "input"
          ],
          "description": "Explicitly set interaction mode (overrides auto-inference from suggested_options). 'confirm' → yes/no buttons, 'select' → option list, 'input' → free text."
        },
        "default_value": {
          "type": "string",
          "description": "Optional default option (confirm/select) or pre-filled text (input)."
        },
        "placeholder": {
          "type": "string",
          "description": "Placeholder text in input field (input mode only)."
        },
        "allow_custom": {
          "type": "boolean",
          "description": "Allow custom input in select mode. Default: true. Set false to restrict to suggested options only."
        }
      },
      "required": [
        "reason",
        "specific_question"
      ]
    }
  },
  {
    "name": "agent_exit",
    "description": "Exit the agent with a reason. Use when done — task completed, failed, needs clarification, or must terminate. Reason must explain WHY.",
    "parameters": {
      "type": "object",
      "properties": {
        "reason": {
          "type": "string",
          "description": "Reason for exiting, e.g., task completed, cannot proceed, blocked by missing info, or must terminate."
        }
      },
      "required": [
        "reason"
      ]
    }
  },
  {
    "name": "delegate_browser",
    "description": "Delegate web research to Browser-Agent. Use ONLY when info CANNOT be obtained from local docs (go docs, python docs, help, man, --help). Browser-Agent navigates the web and returns findings. Provide a clear, self-contained task description.",
    "parameters": {
      "type": "object",
      "properties": {
        "task": {
          "type": "string",
          "description": "Clear, self-contained research task, e.g., 'Search for the latest docs on Python's asyncio.run' or 'Find solutions for Go error: undefined: sql.NullString'."
        }
      },
      "required": [
        "task"
      ]
    }
  },
  {
    "name": "find_function_callee",
    "description": "Find all functions a specified function calls (its callees). Returns invoked functions with file paths and line numbers. Understands what a function does internally.",
    "parameters": {
      "type": "object",
      "properties": {
        "function_name": {
          "type": "string",
          "description": "Function name to find callees for."
        },
        "filepath": {
          "type": "string",
          "description": "Optional file path to narrow search (e.g., 'src/main.go')."
        }
      },
      "required": [
        "function_name"
      ]
    }
  },
  {
    "name": "find_function_caller",
    "description": "Find all functions that call a specified function (its callers). Returns invoking functions with file paths and line numbers. Understands who depends on this function.",
    "parameters": {
      "type": "object",
      "properties": {
        "function_name": {
          "type": "string",
          "description": "Function name to find callers for."
        },
        "filepath": {
          "type": "string",
          "description": "Optional file path to narrow search (e.g., 'src/main.go')."
        }
      },
      "required": [
        "function_name"
      ]
    }
  },
  {
    "name": "query_call_graph",
    "description": "Query function call graph with configurable depth. Shows callers (upstream) and callees (downstream) around a center function. Depth = layers of callers/callees (max 3). Understands the full calling context.",
    "parameters": {
      "type": "object",
      "properties": {
        "function_name": {
          "type": "string",
          "description": "Function name to query as the center of the call graph."
        },
        "depth": {
          "type": "integer",
          "description": "Layers of callers/callees to include (1-3, default: 1).",
          "default": 1,
          "minimum": 1,
          "maximum": 3
        }
      },
      "required": [
        "function_name"
      ]
    }
  },
  {
    "name": "git_checkpoint_list",
    "description": "List all git checkpoints for the current session. See available rollback points. Each represents a saved codebase state to return to if something goes wrong.",
    "parameters": {
      "type": "object",
      "properties": {},
      "required": []
    }
  },
  {
    "name": "git_checkpoint_rollback",
    "description": "Roll back working tree to a specific checkpoint. WARNING: discards all changes AFTER the checkpoint. Use git_checkpoint_list first. checkpoint_tag must be the full tag name from the list.",
    "parameters": {
      "type": "object",
      "properties": {
        "checkpoint_tag": {
          "type": "string",
          "description": "Tag name of checkpoint to roll back to (from git_checkpoint_list)."
        }
      },
      "required": [
        "checkpoint_tag"
      ]
    }
  },
  {
    "name": "git_checkpoint_create",
    "description": "Manually create a git checkpoint at the current state. Use before risky operations (large refactors, complex merges, experimental changes). Allows rollback if something goes wrong.",
    "parameters": {
      "type": "object",
      "properties": {
        "message": {
          "type": "string",
          "description": "Why this checkpoint is being created."
        }
      },
      "required": [
        "message"
      ]
    }
  },
  {
    "name": "consolidate_knowledge",
    "description": "Consolidate key knowledge from the current task into the knowledge base. Type and source auto-tagged by the system. Suitable for: (1) distilling domain knowledge after code analysis; (2) recording key change decisions after modifications; (3) capturing architecture patterns or design rules. Content ≤1500 chars, at least 1 tag required.",
    "parameters": {
      "type": "object",
      "properties": {
        "title": {
          "type": "string",
          "description": "Knowledge entry title (≤200 chars).",
          "maxLength": 200
        },
        "content": {
          "type": "string",
          "description": "Knowledge content (≤1500 chars). Keep file paths, function names, and symbol names.",
          "maxLength": 1500
        },
        "tags": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "minItems": 1,
          "description": "Knowledge tag list (≥1 tag)."
        },
        "related_files": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Related file paths (optional)."
        },
        "task_id": {
          "type": "string",
          "description": "Associated task ID (optional)."
        },
        "confidence": {
          "type": "number",
          "description": "Confidence 0-1 (default 1.0).",
          "default": 1.0
        }
      },
      "required": [
        "title",
        "content",
        "tags"
      ]
    }
  },
  {
    "name": "prune_history",
    "description": "Maintain knowledge base health: list entries, merge similar entries, or delete stale entries by ID. Run merge periodically to deduplicate.",
    "parameters": {
      "type": "object",
      "properties": {
        "action": {
          "type": "string",
          "enum": [
            "list",
            "merge",
            "delete"
          ],
          "description": "Operation type: list=List entries, merge=Merge similar entries, delete=Delete by ID"
        },
        "limit": {
          "type": "integer",
          "description": "Max entries to read for list/merge (default: 50).",
          "default": 50
        },
        "type": {
          "type": "string",
          "description": "Filter by knowledge type (optional, empty=all)."
        },
        "tag": {
          "type": "string",
          "description": "Filter by tag (optional)."
        },
        "ids": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "List of entry IDs to delete (required for delete)."
        },
        "similarity_threshold": {
          "type": "number",
          "description": "Similarity threshold for merge (default: 0.80).",
          "default": 0.8
        }
      },
      "required": [
        "action"
      ]
    }
  }
]
